<h1>Welcome to Half Stack!</h1>

<p>Your number one place for half answers.</p>



<p>Here are the questions needing answers:</p>
  <% if @posts.any? %>
  <% @posts.each do |post| %>
  <div class="row">
    <div class="col s12 m6">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <span class="card-title"><small><%= User.find(post.user_id).user_name %></small></span>
          <%= link_to post.content.truncate(50), user_post_path(current_user.id, post.id) %>
        </div>
        <div class="card-action">
          <%= link_to 'Edit', edit_user_post_path(current_user.id, post.id) %>
          <%= link_to 'Delete', user_path(post.id), :data => {:confirm => "You sure?", :method => "delete"}  %>
        </div>
      </div>
    </div>
  </div>
  <% end %>
  <% else %>
  <%= link_to "Create new Post", new_user_post_path(current_user.id) %>
  <% end %>
